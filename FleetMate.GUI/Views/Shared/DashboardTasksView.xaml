<UserControl x:Class="FleetMate.GUI.Views.Shared.DashboardTasksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019">
    <!-- One table, not one per provider: DevOps work items and GitHub issues share
         it, and the dashboard's PR source chips decide which rows show. -->
    <Border Style="{StaticResource CardStyle}" Padding="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <DockPanel Grid.Row="0" Margin="12,10,12,6">
                <TextBox x:Name="RowSearch" DockPanel.Dock="Right" Width="160" FontSize="12"
                         ui:ControlHelper.PlaceholderText="Filter..." TextChanged="OnSearchChanged" />
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Work Items" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center" />
                    <TextBlock x:Name="CountText" FontSize="11" Margin="6,0,0,0" VerticalAlignment="Center"
                               Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />
                </StackPanel>
            </DockPanel>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="320">
                <StackPanel x:Name="RowsPanel" Margin="0,0,0,6" />
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>

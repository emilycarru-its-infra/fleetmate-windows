<Window x:Class="FleetMate.GUI.Views.Manage.TextPromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Prompt" Width="420" SizeToContent="Height" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="18" TextElement.Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptLabel" Grid.Row="0" Margin="0,0,0,6" Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />
        <TextBox x:Name="ValueBox" Grid.Row="1" KeyDown="OnKeyDown" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Cancel" Margin="0,0,8,0" Padding="16,5" IsCancel="True" />
            <Button x:Name="OkButton" Content="OK" Padding="16,5" IsDefault="True" Style="{StaticResource AccentButtonStyle}" Click="OnOk" />
        </StackPanel>
    </Grid>
</Window>

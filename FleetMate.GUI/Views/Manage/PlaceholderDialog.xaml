<Window x:Class="FleetMate.GUI.Views.Manage.PlaceholderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="Fill in the command" Width="560" SizeToContent="Height" MinWidth="460" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False">
    <Grid Margin="18" TextElement.Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="TitleText" Grid.Row="0" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,10" TextWrapping="Wrap" />
        <StackPanel x:Name="Fields" Grid.Row="1" />
        <StackPanel Grid.Row="2" Margin="0,10,0,0">
            <TextBlock Text="Command preview" FontSize="11" Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />
            <TextBox x:Name="PreviewBox" IsReadOnly="True" TextWrapping="Wrap" FontFamily="Consolas" FontSize="12" Margin="0,4,0,0" MaxHeight="140" VerticalScrollBarVisibility="Auto" />
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="Cancel" Margin="0,0,8,0" Padding="16,5" IsCancel="True" />
            <Button x:Name="RunButton" Content="Run" Padding="16,5" IsDefault="True" Style="{StaticResource AccentButtonStyle}" Click="OnRun" />
        </StackPanel>
    </Grid>
</Window>
